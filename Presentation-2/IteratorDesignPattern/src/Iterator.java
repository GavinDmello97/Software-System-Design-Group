// Interator interface
public interface Iterator {
  // hasNext is used to check if there is a next element in the list
  public boolean hasNext();

  // next is used to fetch the next element in the list
  public void next();
}
